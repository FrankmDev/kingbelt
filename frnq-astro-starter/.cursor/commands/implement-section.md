# Implement one section

Implement exactly one named section. Before editing, state:

- allowed files and prohibited files;
- relevant visual reference and its assigned use;
- mobile, intermediate, and desktop behavior;
- acceptance criteria.

Use the page plan, `DESIGN.md`, and `ARCHITECTURE.md`. Reuse Tailwind theme tokens and narrow primitives. Compose the section with utilities and do not create another CSS file. Run `bun run check`, inspect the real page at mobile and desktop with available browser tools, and report files, checks, visual findings, and deferred work. Do not continue into adjacent sections.
