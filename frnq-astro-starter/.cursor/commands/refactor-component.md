# Refactor component

Define the repeated problem and observable behavior to preserve. List consumers and allowed files. Refactor one responsibility at a time; do not create speculative variants or catch-all props. Run check, build when structure changes, affected tests, and visual comparison. Stop if the change expands beyond the named contract.
