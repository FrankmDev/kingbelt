# Run QA

Read `docs/project/QA.md`. Run `bun run check`, `bun run build`, and any project-specific checks documented there. Inspect real navigation, forms, overflow, focus, mobile, and desktop with available browser tools. Report exact commands, viewports, passed checks, failures by cause, and limitations.
