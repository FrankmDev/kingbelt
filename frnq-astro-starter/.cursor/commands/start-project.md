# Start project

Read `project.config.ts`. If `meta.initialized` is false, read `PROJECT_START.md` completely and execute all nine initialization phases. Read references before asking for missing information. Generate the project documents, Tailwind theme, architecture, foundation, required functionality, and QA criteria without implementing finished pages. Validate fully, then and only then set `initialized: true`. Return the initialization report required by Phase 9.
